const ApplicationProcessCount = require('./applicationProcessCount');
const BluetoothBond = require('./bluetoothBond');
const CallDuration = require('./callDuration');

const models = {
  ApplicationProcessCount,
  BluetoothBond,
  CallDuration,
};

module.exports = models;
